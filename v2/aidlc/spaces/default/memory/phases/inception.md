# Inception phase rules — FEDzk

- Reverse engineering MUST scan real code (not README claims).
- Every architecture decision documents ≥2 alternatives (AIDLC inception default).
- Application design must preserve verify-before-aggregate as the invariant.
- Units generation produces a DAG: Phase0 hygiene → circuits → SDK → rust engine → experiments.
- Requirements trace to REQUIREMENTS.html IDs (FR-*, NFR-*, SEC-*, RS-*).
