# Operation phase rules — FEDzk

- Skipped under AIDLC `mvp` scope until walking skeleton + paper experiments land.
- When promoted to `feature`/`enterprise`: Helm probes use HTTP healthz; Node-only compile Job separate from prove Deployment.
- Feedback loop returns operational prove latency / reject rates to product intent (PAPER metrics).
